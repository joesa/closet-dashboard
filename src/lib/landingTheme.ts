/* ─────────────────────────────────────────────────────────────────────
   Landing palette — light, product-first. One accent (ultramarine),
   ink buttons, hairline borders, layered shadows via .landing-shadow-*.
   Shared by the marketing homepage and the intake flow so both read
   as one product. The rest of the app is dark.
   ──────────────────────────────────────────────────────────────────── */
export const INK = '#10141A'
export const INK_2 = '#4E5761'
// Secondary text still has to carry pricing terms, labels, and links. The old
// #8B939C only reached ~3:1 on the landing surfaces and caused dozens of live
// WCAG failures; this keeps the quiet role while clearing AA at small sizes.
export const INK_3 = '#626B75'
export const BG = '#FBFBFA'
export const SURFACE_2 = '#F4F5F4'
export const HAIRLINE = '#E7E8E8'
export const HAIRLINE_2 = '#D8DADB'
export const ACCENT = '#2438C9'
export const ACCENT_SOFT = '#EDEFFB'
